# OOP-foundations
1. What is the role of a class in a linked list?
- Class is used to create the structure of nodes and linked list.
2. What is the difference between a node and a linked list?
- A node has a piece of data and a link to the next node.
- A linked list is a collection of linked nodes.
3. Why do we use None in next?
- None means there is no next node.
4. How is a linked list different from a Python list?
- Indexing a Python list. Linked list connects the nodes using links.
7. Why is OOP useful for data structures?
- OOP helps organize code, making it easier to manage and reuse.
  
